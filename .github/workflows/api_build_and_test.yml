name: API - Build and test

on:
  push:
  pull_request:
    types:
      - opened
    branches:
      - 'main'

env:
  PROJECT-PATH: ./src/Host

jobs:
  build-and-test:

    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Set up .NET Core
        uses: actions/setup-dotnet@v1
        with:
          dotnet-version: '7.x'

      - name: Restore
        run: dotnet restore

      - name: Build
        run: dotnet build ${{env.PROJECT-PATH}} --configuration Release --no-restore

      - name: Test
        run: dotnet test --no-restore --verbosity normal
